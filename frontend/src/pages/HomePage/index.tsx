import { Page } from '../../components/Page';
import { TermSearch } from '../../components/TermSearch';

export const HomePage = () => {
  return (
    <Page pageTitle="Home">
      <TermSearch />
    </Page>
  );
};
